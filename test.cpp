
#include <iostream>
#include <vector>
#include <algorithm>
#include <cstdio>

using namespace std;

struct Vec2 {
    int x;
    int y;
    Vec2(int x, int y): x(x), y(y) {}
};

int main() {
    float t = 0 / float(4);
    int y = 0 + t * 4;
    printf("")
}
